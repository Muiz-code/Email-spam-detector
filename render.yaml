services:
  - type: web
    name: email-spam-detector
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn spamDetection.spamDetection.wsgi:application
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: spamDetection.settings
